import heroTitleStyles from "../styles/heroTitle.module.scss";


const HeroTitle = () => {
    return (
        <div>
            <h1 className={heroTitleStyles.title}>Noah Beij</h1>
        </div>
    );
}

export default HeroTitle;